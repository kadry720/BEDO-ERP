[build]
builder = "DOCKERFILE"
dockerfilePath = "infrastructure/railway/frappe.Dockerfile"

[deploy]
startCommand = "bash infrastructure/railway/start-web.sh"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
